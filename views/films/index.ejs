<%- include('../partials/header') %>
  
<% films.forEach(function(f) { %>
   
    <tr>
      <td><%= f.title %></td>
      <td><a href="/films/<%= f._id %>">DETAILS</a></td>
    </tr>
    <% }); %>
  </tbody>
</table>

<%- include('../partials/footer') %>
